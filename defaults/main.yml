---

pacman_options:
  XferCommand: /usr/bin/wget --passive-ftp -c -O %o %u

pacman_conf: /etc/pacman.conf
pacman_mirrorlist: /etc/pacman.d/mirrorlist
pacman_mirrors: []
pacman_repositories: []
pacman_indempotence_path: '{{ ansible_env.HOME }}/.jpic.pacman'
